import { CSSProperties, FC } from 'react'
import { colors } from "../../data/colors"

interface Props {

}

const cartContext: FC<Props> = (props) => {
    return (
        <div>
        
        </div>
    )
}

export default cartContext


const background: CSSProperties = {
    backgroundColor: colors.primary
    
}
