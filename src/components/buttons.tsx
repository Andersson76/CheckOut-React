import { FC } from "react"

interface Props {
/*     submit: 
    addProduct: 
    deleteProduct:
    cart: */
    
}

const Buttons: FC<Props> = (props) => {

    return (
        <div>
        <h1>information</h1>
        </div>
    )
}

// Button-hanterare 

{/* <button onClick={}>
    Lägg till i varukorgen
</button> */}

export default Buttons