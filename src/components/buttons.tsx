import { FC } from "react"
export interface Props {
    
}

const Buttons: FC<Props> = (props) => {

    return (
        <div>
        <h1>information</h1>
        </div>
    )
}

export default Buttons